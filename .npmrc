engine-strict=true
# Workspace packages are linked through explicit `workspace:` specifiers only,
# so an accidental version drift fails loudly instead of resolving silently.
link-workspace-packages=false
save-exact=true
