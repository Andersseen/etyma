pnpm-lock.yaml
dist/
.turbo/
.angular/
.nitro/
.output/
coverage/
playwright-report/
test-results/
.wrangler/
tools/compat/*/node_modules/
CHANGELOG.md

# Generated by @etyma/tooling's etymaRemoteContract Vite plugin - deterministic, tool-owned,
# and never hand-formatted, so it can be diffed for actual content changes only.
**/*.generated.ts

# Deliberately invalid JSON, fixturing @etyma/cli's "malformed catalog file" error path -
# Prettier trying to reformat it is the same category of mistake as `validate` trying to
# parse it as a catalog.
packages/cli/src/__fixtures__/malformed-json/es.json
