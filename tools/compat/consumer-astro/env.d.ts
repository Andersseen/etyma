/**
 * Where this fixture's catalogs are served during `astro build`: a loopback server started by
 * `astro.config.mjs`, injected through Vite's `define` so page code needs no Node types.
 */
declare const __ETYMA_COMPAT_CATALOGS__: string;
